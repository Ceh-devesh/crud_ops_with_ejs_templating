<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body >
    <div class="my-4 ml-4    p-4 rounded-md border-2 inline-block">
        <a class="text-blue-500 " href="/create"><button>Create a user</button></a>
    </div>
    

    <% user.forEach((user)=>{ %>
        <div class=" flex flex-wrap gap-5 m-5 " >
    <div class="bg-white shadow-md rounded-lg p-6 w-80">
        <div class="mb-4">
            <h3 class="text-xl font-semibold"> <%= user.name%> </h3>
            <h3 class="text-xl font-semibold"><%= user.age %></h3>
            <h3 class="text-xl font-semibold"><%= user.location %></h3>
        </div>
        <div class="flex justify-end space-x-4">
                <a class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600 transition " href="/update/<%= user._id %>">
                    Edit</a>
                    
                <a class="bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600 transition" href="/delete/<%= user._id %>">Delete</a>
            
           
        </div>
    </div>
</div>
   <% }) %>
</body>
</html>